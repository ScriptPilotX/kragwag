# Arduino build output
firmware/build/
build/
firmware-build/

# Local compile logs from the release skill
compile_out.txt
compile_stderr.txt
compile_stdout.txt

# Zipped source backups (the .ino itself is tracked)
*.ino.zip
 
# auto-saved by the Claude desktop app 
Claude outputs/
